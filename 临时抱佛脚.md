# OS C Code 考前极简检查表

> 只看代码题。目标：考试时快速找**普遍错误**，不要纠结特殊业务场景。

---

# 1. Mutex / Thread Code

## A. 看到共享变量，先想 mutex

只要多个 thread 读写同一个变量/数组/链表/栈，就要锁。

常见共享对象：

```c
count
request_count
head
list
array[i]
struct里的状态字段
```

**答题句：**

```text
This shared data can be accessed by multiple threads concurrently, causing a race condition.
Protect all accesses to it using the same mutex.
```

---

## B. 同一个数据结构必须用同一把锁

错：

```c
push 用 m1
pop 用 m2
```

改：

```c
push / pop / is_empty 全部用同一把 mutex
```

**核心：同一个 count 不能被两把不同锁分别保护。**

---

## C. lock 后必须保证 unlock 会执行

错：

```c
pthread_mutex_lock(&m);
return count == 0;
pthread_mutex_unlock(&m);
```

改：

```c
pthread_mutex_lock(&m);
int result = (count == 0);
pthread_mutex_unlock(&m);
return result;
```

**口诀：不要在 unlock 前 return。**

---

## D. 临界区只包共享数据，不包慢操作

如果 `get()` / `handle()` / I/O / 复杂计算不需要保护，就不要放锁里面。

推荐结构：

```c
data = get();              // 不锁，允许并行

pthread_mutex_lock(&m);
shared[count] = data;
count++;
pthread_mutex_unlock(&m);
```

消费也一样：

```c
pthread_mutex_lock(&m);
count--;
data = shared[count];
pthread_mutex_unlock(&m);

handle(data);              // 不锁，允许并行
```

**答题句：**

```text
The critical section should be as short as possible to maximise parallelism.
```

---

## E. pthread_create 不要传 loop variable 的地址

错：

```c
for (int i = 0; i < 4; i++)
    pthread_create(&tid[i], NULL, foo, &i);
```

原因：所有 thread 拿到的是同一个 `i` 的地址。

改：

```c
int ids[4];

for (int i = 0; i < 4; i++) {
    ids[i] = i;
    pthread_create(&tid[i], NULL, foo, &ids[i]);
}
```

---

## F. 创建多个 thread 要保存多个 tid

错：

```c
pthread_t tid;
for (...) pthread_create(&tid, NULL, f, arg);
```

改：

```c
pthread_t tid[N];

for (int i = 0; i < N; i++)
    pthread_create(&tid[i], NULL, f, arg);

for (int i = 0; i < N; i++)
    pthread_join(tid[i], NULL);
```

---

## G. 基本 mutex 模板

```c
pthread_mutex_t m = PTHREAD_MUTEX_INITIALIZER;

pthread_mutex_lock(&m);
/* access shared data */
pthread_mutex_unlock(&m);
```

---

# 2. 普通 C Code

## A. malloc / calloc 后检查 NULL

```c
p = malloc(sizeof(*p));

if (p == NULL) {
    return NULL;   // 或 exit(1)
}
```

**不要写错对象：**

```c
if (head == NULL)   // 错，应该检查刚 malloc 的 p
```

---

## B. 结构体节点必须分配在 heap，不能返回局部变量地址

错：

```c
struct Node newNode;
return &newNode;
```

或：

```c
prev->next = &newNode;
```

改：

```c
struct Node *newNode = malloc(sizeof(*newNode));
```

**口诀：函数结束还要活着的东西，用 malloc。**

---

## C. free 只能 free malloc/calloc/realloc 出来的指针

错：

```c
free(node->value);   // value 是 int，不是 heap pointer
```

对：

```c
free(node);
```

如果字段是 `char *name = malloc(...)`，才需要：

```c
free(node->name);
free(node);
```

---

## D. free 链表/树前，先保存 next / 先递归 children

链表错：

```c
free(p);
p = p->next;     // p 已经被 free 了
```

改：

```c
while (p != NULL) {
    Node *next = p->next;
    free(p);
    p = next;
}
```

树：

```c
void deleteNode(Node *node) {
    if (node == NULL) return;

    deleteNode(node->left);
    deleteNode(node->right);
    free(node);
}
```

---

## E. 访问指针前先检查 NULL

错：

```c
while (current->id < newID)
```

改：

```c
while (current != NULL && current->id < newID)
```

---

## F. 字符串不能直接 sprintf 到未分配的 char*

错：

```c
char *arg;
sprintf(arg, "hello");
```

改：

```c
char *arg = malloc(strlen(s) + 20);
sprintf(arg, "hello %s", s);
```

更安全：

```c
snprintf(arg, size, "hello %s", s);
```

---

## G. strcpy 可能 buffer overflow

如果目标是固定数组：

```c
char title[80];
strcpy(title, newTitle);   // newTitle 太长会爆
```

更安全：

```c
strncpy(title, newTitle, 79);
title[79] = '\0';
```

如果题目说字符串任意长度，更好改成：

```c
char *title = malloc(strlen(newTitle) + 1);
strcpy(title, newTitle);
```

---

## H. 插入链表要处理 empty list / insert at head

必须考虑：

```c
if (head == NULL)
```

以及：

```c
if (new node should be before head)
```

改法常见：

```c
newNode->next = head;
head = newNode;
```

如果函数需要改外面的 head，用：

```c
Node **head
```

或 return 新 head：

```c
return head;
```

---

## I. 数组边界检查

错：

```c
arr[count] = x;
count++;
```

要先检查：

```c
if (count < SIZE) {
    arr[count] = x;
    count++;
}
```

pop 类也要检查：

```c
if (count > 0) {
    count--;
    return arr[count];
}
```

---

## J. realloc 用临时指针

错：

```c
p = realloc(p, newSize);
```

如果失败，原来的 `p` 丢了。

改：

```c
tmp = realloc(p, newSize);
if (tmp != NULL) {
    p = tmp;
}
```

---

# 3. 考试 30 秒扫代码顺序

1. 有没有多个 thread 共享变量？  
   → 加 mutex。

2. 是不是同一个数据结构用了不同 mutex？  
   → 改成同一把锁。

3. lock 后有没有提前 return？  
   → return 前必须 unlock。

4. pthread_create 有没有传 `&i`？  
   → 改成 `ids[i]`。

5. 有没有 `char *p; sprintf(p, ...)`？  
   → p 要 malloc。

6. 有没有 `strcpy` 到固定数组？  
   → 检查 overflow。

7. 有没有局部 struct 被挂到链表/return？  
   → 改 malloc。

8. 有没有 free 后还访问？  
   → 先保存 next，再 free。

9. 有没有 `free(int)` / free 非 malloc 内存？  
   → 删除错误 free。

10. 有没有访问 `p->x` 前没检查 `p != NULL`？  
    → 加 NULL check。

---

# 4. 最好背的万能句

```text
This code has undefined behaviour because it accesses invalid memory.
```

```text
The pointer may be NULL, so it must be checked before dereferencing.
```

```text
The memory must be allocated dynamically because it is used after the function returns.
```

```text
This shared variable is accessed by multiple threads, causing a race condition.
```

```text
All accesses to the shared data structure should be protected by the same mutex.
```

```text
The lock should be released before returning from the function.
```

```text
The critical section should be kept short to maximise parallelism.
```
